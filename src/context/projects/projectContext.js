// Imported into ProjectState, NewProject, ProjectsList, Project, TasksList, NewTask.
import { createContext } from "react";

// Creates context and exports it for usage in all App components.
const projectContext = createContext();

export default projectContext;

// Imported into TaskState, NewTask, Task.
import { createContext } from "react";

// Creates context and exports it for usage in all App components.
const taskContext = createContext();

export default taskContext;
